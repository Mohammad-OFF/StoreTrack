version: "3.9"

services:
  app:
    build: .
    container_name: storetrack_app
    restart: unless-stopped
    environment:
      DB_HOST: localhost
      DB_USER: appuser
      DB_PASSWORD: apppass
      DB_NAME: storage_db
      PORT: 3000
    ports:
      - "3000:3000"
